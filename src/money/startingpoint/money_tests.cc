#include <gtest/gtest.h>
#include "gmock/gmock.h"
#include "money.h"

using namespace money;

namespace
{

    TEST(MoneyTests, TrueShouldBeTrue)
    {
        EXPECT_EQ(true,true);
    }

}